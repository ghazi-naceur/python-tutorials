def cap_text(text):
    # strings = []
    # for item in text.split(" "):
    #     strings.append(item.capitalize())
    # return " ".join(strings)

    # The previous logic could be replaced by :
    return text.title()
